<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meet the Floofs | Baby Floofs</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700&family=Nunito:wght@400;600&display=swap"
        rel="stylesheet">

    <link rel="stylesheet" href="/style-pets.css">
</head>

<body>

    <nav class="navbar navbar-expand-lg custom-navbar sticky-top">
        <div class="container-fluid container-xl">
            <a class="navbar-brand brand-name" href="/">🐾 Baby Floofs</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/pets">Meet the Pets</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <header class="text-center py-5">
        <h1 class="showcase-title fw-bold">Meet Your Paw-fect Companions 🐾</h1>
        <p class="lead tagline-text">Handpicked floofs waiting to steal your heart.</p>
    </header>

    <section class="container py-4">
        <div class="row g-4">
            <% for (let id in pets) { %>
                <div class="col-sm-6 col-md-4 col-lg-3">
                    <div class="pet-card" data-id="<%= id %>">
                        <img src="<%= pets[id].image %>" class="pet-img">
                        <div class="pet-info">
                            <h5>
                                <%= pets[id].name %>
                            </h5>
                            <p>
                                <%= pets[id].breed %>
                            </p>
                            <p>
                                <%= pets[id].caption %>
                            </p>
                        </div>
                    </div>
                </div>
                <% } %>
        </div>

    </section>

    <script src="/script-pets.js"></script>
</body>

</html>