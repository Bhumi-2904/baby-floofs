<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pet.name %> | Baby Floofs</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700&family=Nunito:wght@400;600&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/style-pet-details.css">
</head>

<body>

    <nav class="navbar navbar-expand-lg custom-navbar sticky-top">
        <div class="container-fluid container-xl">
            <a class="navbar-brand brand-name" href="/">🐾 Baby Floofs</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-expanded="false">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/pets">Meet the Pets</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <section class="container py-5 pet-detail-container">

        <div class="row align-items-center g-5">

            <div class="col-lg-6 text-center">
                <img src="<%= pet.image %>" 
                     alt="<%= pet.name %>" 
                     class="pet-detail-img rounded-4 shadow-lg" />
            </div>

            <div class="col-lg-6">
                <h1 class="pet-detail-name fw-bold">
                    <%= pet.name %> 
                    <span class="badge rounded-pill pet-type-badge"><%= pet.type %></span>
                </h1>

                <p class="pet-detail-caption"><%= pet.caption %></p>

                <ul class="list-unstyled pet-info-list mt-4">
                    <li><strong>Breed:</strong> <%= pet.breed %></li>
                    <li><strong>Age:</strong> <%= pet.age %> years old</li>
                </ul>

                <p class="pet-detail-description mt-3"><%= pet.description %></p>

                <a href="/pets" class="btn btn-lg back-btn rounded-pill mt-4">← Back to Floofs</a>
            </div>

        </div>

    </section>

    <script src="/script-pet-details.js"></script>
</body>
</html>
